# 更新日志

本文件记录 Telegram Search Native 的版本演进。所有 APK 为调试签名构建，并在 GitHub Releases 中提供对应的 SHA-256 校验文件。

> 仓库根目录保留当前可构建的 v1.7.0 源码；`archive/` 包含 v0.1.0 与 v0.2.0 的源码快照。未单独保留源码快照的历史版本以 APK、校验文件和版本说明进行归档。

| 版本 | APK | 主要更新 |
|---|---|---|
| v1.7.0-api-priority | 有 | 自定义 API 优先、默认 API 回退、错误来源提示与恢复默认入口。 |
| v1.6.0-saved-search | 有 | 收藏页多关键词本地搜索、清除操作与空结果提示。 |
| v1.5.0-build-api-privacy | 有 | 可选构建时自定义 API 注入；设置页 API 凭据显示/隐藏。 |
| v1.4.0-multi-page-slider | 有 | 收藏页与同步页增加右侧垂直滑块快速定位。 |
| v1.3.1-time-list | 有 | 移除 # 与字母分组，搜索结果按时间顺序显示。 |
| v1.3.0-multi-select | 有 | 搜索结果长按多选、全选、批量本机收藏与批量删除。 |
| v1.2.0-index-manager | 有 | 搜索索引管理、全部索引删除与按会话删除。 |
| v1.1.0-search-slider | 有 | 右侧垂直滑块、宽触摸热区、进度浮层与快速定位。 |
| v1.0.1-pointer-overlay | 有 | 覆盖层连续触摸追踪，修复右侧字母区滑动。 |
| v1.0.0-drawer-index | 有 | 紧凑抽屉式字母索引、长按滑动浮层和搜索列表滚动优化。 |
| v0.9.1-index-drag-fix | 有 | 修复右侧字母索引条上下拖动跳转。 |
| v0.9.0-search-index-rail | 有 | 搜索页右侧 #–Z 索引、会话名称分组和上下拖动快速定位。 |
| v0.8.1-multi-keyword-search | 有 | 多关键词全部包含检索，支持空格和常见分隔符。 |
| v0.8.0-local-library | 有 | 本机收藏夹、单条本地删除与本地数据清除。 |
| v0.1.0-local-import | 有 | JSON/JSONL 本地导入与离线检索。 |
| v0.2.0-tdlib | 有 | TDLib 登录、会话同步与本地搜索。 |
| v0.2.1-code-retry | 有 | 验证码错误恢复与认证状态提示。 |
| v0.3.0-full-history | 有 | 分页历史同步。 |
| v0.3.1-history-icon | 无单独 APK | 启动图标设计迭代。 |
| v0.3.2-sync-icon | 有 | 同步稳定性、媒体配文索引与图标更新。 |
| v0.4.0-chat-picker | 有 | 会话名称搜索与已选会话置顶。 |
| v0.5.0-message-actions | 有 | Saved Messages 转发、Telegram 跳转与导航体验。 |
| v0.5.1-message-refresh-copy | 有 | 消息权限刷新、文本选择与全文复制。 |
| v0.6.0-scroll-pagination-fix | 有 | 长消息滚动与历史分页游标优化。 |
| v0.7.0-continuous-sync | 有 | 持续历史同步与分页进度显示。 |

## v1.7.0-api-priority

API 配置逻辑现在会明确识别参数来源。设备内手动保存的自定义 API 始终优先；仅在没有自定义参数时才使用构建时注入的默认 API。TDLib 初始化阶段若返回 API ID 或 API Hash 相关错误，连接页会指出失败的是自定义 API 还是构建默认 API，并提供相应处理建议。为避免掩盖用户输入错误，自定义 API 被拒绝后不会静默回退；在含构建默认参数的版本中，用户可从设置页确认清除自定义 API，再恢复默认配置。该操作不会删除本地索引、收藏或 Telegram 登录数据。

## v1.6.0-saved-search

本机收藏页现在支持与主搜索页相同的多关键词包含检索。输入框支持空格、逗号、顿号、分号、竖线或斜杠分隔多个词，结果只会显示同时包含全部关键词的本机收藏消息。输入关键词后会自动回到列表顶部，显示筛选结果数量；可通过末尾清除按钮恢复全部收藏。右侧垂直滑块继续基于当前筛选后的收藏结果快速定位，未匹配时会显示单独提示。

## v1.5.0-build-api-privacy

公开源码新增可选的 Telegram API 构建注入机制。克隆者可通过受 Git 忽略的 `local.properties`、环境变量或 Gradle 属性提供自己的 `TG_DEFAULT_API_ID` 和 `TG_DEFAULT_API_HASH`；构建默认值仅在设备中尚未保存手动参数时使用，现有“设置”页手动填写和 Android Keystore 加密存储仍保持不变。设置页的 API ID 与 API Hash 现在默认遮蔽，并可通过末尾小眼睛按钮临时显示或隐藏。

## v1.4.0-multi-page-slider

右侧垂直滑块快速定位现已覆盖本机收藏页和选择会话并同步页。收藏数量超过一条时，可通过滑块快速跳转到目标收藏消息；会话列表在未同步时也可通过滑块浏览当前可见的已选会话与搜索结果。拖动期间会显示位置百分比，普通列表滚动时滑块同步更新。同步执行期间，会话勾选与滑块均保持锁定，完成或停止同步后即可继续浏览。

## v1.3.1-time-list

移除搜索结果页的 # 与字母分组标题。结果统一按消息时间从新到旧显示，右侧垂直滑块按该时间序列的位置快速定位；同时移除了会话名称分组带来的额外排序开销。

## v1.3.0-multi-select

搜索结果页支持长按进入多选模式，点击消息可勾选或取消勾选。顶部操作条提供全选、取消全选、批量加入本机收藏、批量删除本机索引和退出选择模式；使用系统返回键也可退出。批量删除操作需要确认，全部操作仅影响当前设备上的本地索引。

## v1.2.0-index-manager

设置页新增搜索索引管理入口，显示本机索引消息、会话和收藏统计。支持通过确认操作删除全部本机索引，也可展开会话列表并单独删除目标会话的索引；删除后搜索结果、收藏和统计即时刷新，不影响 Telegram 账号中的原始消息。

## v1.1.0-search-slider

移除密集字母索引，改为右侧垂直滑块。滑块以列表位置比例定位搜索结果，触摸区域扩展至 48dp；拖动时显示当前位置百分比并使用无动画列表项跳转，正常滚动列表时滑块同步更新位置。

## v1.0.1-pointer-overlay

字母区改为原始指针事件覆盖层追踪：按下、移动和释放都会在同一触摸序列内处理。索引区域主动消费触摸事件，持续将手指纵向位置映射到最近的有效分组，避免子级点击组件或拖动阈值中断滑动。

## v1.0.0-drawer-index

搜索页右侧索引改为紧凑字母列。长按后上下滑动会显示当前命中的字母浮层，并使用直接定位跳转至有效分组。会话分组、排序和目标索引改为在搜索结果变化时缓存计算，减少普通列表滑动过程中的重复工作。

## v0.9.1-index-drag-fix

索引条的拖动事件改为通用手势处理。触摸开始和后续上下移动均会计算最近的有效分组，并在进入新分组时立即定位列表；无数据字母会自动跳过，单个字母点击定位保持可用。

## v0.9.0-search-index-rail

搜索结果按会话名称的英文字母首字母分组；非英文字母开头、数字、符号和空名称归入 `#` 分组。右侧 #–Z 索引条会显示有效分组，点击或上下拖动可快速定位到目标分组，当前可见分组会高亮显示。

## v0.8.1-multi-keyword-search

本地搜索支持多个关键词的全部包含匹配。关键词可使用空格、逗号、中文逗号、顿号、分号、中文分号、竖线或斜杠分隔；消息须同时包含全部关键词才会出现在结果中。单个关键词检索方式保持不变。

## v0.8.0-local-library

新增独立的本机收藏夹入口。收藏消息保存在当前设备的 SQLite 索引中，可从搜索结果或消息详情加入、查看和移除，不与 Telegram Saved Messages 同步。消息详情支持单条本地删除；设置页支持确认后清除全部本机消息、搜索索引和本机收藏，Telegram 账号中的原始消息不受影响。

## v0.7.0-continuous-sync

采用连续历史分页策略：以每页最早消息 ID 作为后续请求游标，使用 `chatId + messageId` 对重叠消息去重。Telegram 返回空页时完成当前会话同步；重复页或游标暂未推进时，应用会继续请求并显示页码进度。

## v0.6.0-scroll-pagination-fix

消息详情页支持整页垂直滚动。历史同步优化分页游标和重复页处理，提升连续读取更早消息的稳定性。

## v0.5.1-message-refresh-copy

消息操作前刷新目标消息及权限属性。详情页支持系统文本选择和全文复制，并在可用时提供转发与 Telegram 链接操作。

## v0.5.0-message-actions

消息详情增加 Saved Messages 转发与“在 Telegram 中打开”。搜索结果和详情页之间保留原有列表位置，Android 返回键优先关闭详情页。

## v0.4.0-chat-picker

同步页面支持会话名称检索、已选会话置顶和已选数量显示，便于管理同步范围。

## v0.3.2-sync-icon

完善会话标题加载和历史同步逻辑，媒体消息的 caption 可进入本地索引，并更新深靛蓝对话检索主题启动图标。

## v0.3.1-history-icon

完成启动图标设计迭代；相关视觉改动已包含在 v0.3.2。

## v0.3.0-full-history

引入已选会话的分页历史读取，为本地历史消息索引提供连续同步能力。

## v0.2.1-code-retry

完善 `PHONE_CODE_INVALID`、`PHONE_CODE_EXPIRED` 和 `PHONE_NUMBER_INVALID` 等认证状态的错误提示与恢复路径。

## v0.2.0-tdlib

引入 TDLib 原生登录状态机，包括手机号、验证码与两步验证，并实现会话选择、文本同步和本地 SQLite 检索。

## v0.1.0-local-import

提供 Telegram 导出 JSON / JSONL 的本地导入、索引和关键词检索能力；不包含 Telegram 登录流程。
