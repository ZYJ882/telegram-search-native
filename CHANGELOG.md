# 更新日志

本文件记录 Telegram Search Native 的交付版本。所有 APK 均为调试签名构建，适合个人测试；版本资产会作为 GitHub Release 附件提供，并附带对应的 SHA-256 校验文件。

> 历史版本反映的是当时的功能节点。仓库根目录始终保留最新可构建的 v0.7.0 源码；早期可用源码快照额外保存在 `archive/`。中间迭代没有单独保存源码快照，因此不会把 v0.7.0 的源码错误标记为早期版本。

| 版本 | APK 资产 | 说明 |
|---|---|---|
| v0.1.0-local-import | 有 | 离线导入 JSON/JSONL 的原型，不涉及 Telegram 登录。 |
| v0.2.0-tdlib | 有 | 首个 TDLib 原生登录、会话同步与本地搜索版本。 |
| v0.2.1-code-retry | 有 | 登录验证码错误恢复。 |
| v0.3.0-full-history | 有 | 分页历史同步。 |
| v0.3.1-history-icon | 无单独 APK | 启动图标设计迭代。 |
| v0.3.2-sync-icon | 有 | 同步修复、媒体配文索引和图标合并。 |
| v0.4.0-chat-picker | 有 | 同步会话搜索、已选置顶。 |
| v0.5.0-message-actions | 有 | 收藏夹、Telegram 跳转、位置与返回行为。 |
| v0.5.1-message-refresh-copy | 有 | 消息权限刷新和复制能力。 |
| v0.6.0-scroll-pagination-fix | 有 | 详情滚动和分页游标修复。 |
| v0.7.0-continuous-sync | 有 | 持续历史同步，当前源码版本。 |

## v0.7.0-continuous-sync

同步流程改为连续历史分页策略。每次从上一页最早消息 ID 继续请求，使用 `offset = 0`，并通过 `chatId + messageId` 对重叠消息去重。只有 Telegram 返回空页时才视为该会话同步结束；遇到重复页或游标暂未推进时不会立即停止，而是短暂等待后继续请求。同步界面显示“第 N 页”进度，用户可主动停止。

## v0.6.0-scroll-pagination-fix

消息详情改为整页垂直滚动，解决长文本无法完整查看的问题。历史同步游标和分页行为调整，避免因偏移设置造成过早停止或重复遗漏。

## v0.5.1-message-refresh-copy

消息操作前会刷新目标消息和聊天权限信息，提升部分公开消息转发、跳转的成功率。详情页支持选择文字，并提供复制全文操作。

## v0.5.0-message-actions

搜索结果详情增加转发到 Saved Messages 和“在 Telegram 中打开”。同时修复搜索列表从详情页返回时的位置恢复，并调整 Android 系统返回逻辑，避免错误退出到桌面。

## v0.4.0-chat-picker

同步页面增加会话名称检索。已选择会话始终置顶，用户勾选会话后自动回到列表顶部，以便确认当前同步范围。

## v0.3.2-sync-icon

修正同步分页和会话标题补取问题，媒体消息的文字说明、配文可进入本地检索索引。引入深靛蓝搜索对话主题的启动图标。

## v0.3.1-history-icon

完成历史同步版本的图标设计与视觉方向验证。该里程碑未单独保留 APK，后续 v0.3.2 已包含图标改动并补充同步修复。

## v0.3.0-full-history

由“近期消息”改为对用户已勾选会话进行分页历史读取，为大量历史内容的本地索引建立基础。

## v0.2.1-code-retry

针对 `PHONE_CODE_INVALID` 等验证码错误提供可恢复的状态提示和重新输入路径，避免发生错误后需要重置整个应用流程。

## v0.2.0-tdlib

首次使用 TDLib 在 Android 端实现 Telegram 登录状态机，包括手机号、验证码、两步验证；同时实现选择会话、同步文本消息与本地 SQLite 检索。

## v0.1.0-local-import

最初的本地原型。支持导入 Telegram 导出的 JSON / JSONL 文本文件，在本地建立索引并搜索；不使用 TDLib，不涉及 Telegram 账号登录。
