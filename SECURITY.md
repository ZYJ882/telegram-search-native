# 安全与隐私说明

## 数据边界

应用不提供自建后端、云端消息同步或遥测上传功能。用户填写的 Telegram 开发者参数、TDLib 授权状态、同步后的消息索引及本地收藏均由 Android 应用私有存储管理。清除应用数据或卸载应用会删除这些本机数据；覆盖安装新版 APK 则通常会保留数据，具体行为取决于 Android 系统和签名一致性。

## 绝不可公开的内容

以下内容应始终仅保留在用户本机，任何一种都不应出现在 Issue、截图、日志、Git 提交、Release 附件或第三方网盘中。

| 内容 | 风险 |
|---|---|
| `api_hash` | 与 API ID 配合后可被用于冒充该开发者应用。 |
| Telegram 验证码、两步验证密码 | 可能导致账号被未经授权地登录。 |
| TDLib 数据库、`tdlib/` 工作目录、会话文件 | 可能包含登录状态、消息元数据或私密缓存。 |
| SQLite 索引、聊天导出 JSON、调试日志 | 可能含有个人消息、群组名称和联系方式。 |
| 签名密钥、`local.properties`、环境变量文件 | 可能泄露发布身份或本地开发配置。 |

## 仓库防护

本仓库的 `.gitignore` 排除构建输出、APK、Android 本地配置、签名材料、TDLib 缓存、SQLite 文件、环境变量和常见会话文件名。GitHub Release 中的 APK 是经过单独上传的构建资产，不被写入 Git 历史。

在执行公开推送前，维护者应至少确认以下事项：项目文件中不存在真实 `api_id` / `api_hash` 数值、不存在手机号和验证码、不存在聊天导出或应用私有目录、不存在签名文件和 `local.properties`，并在 GitHub 的 Secret Scanning 告警出现时立即撤销和轮换相应凭据。

## 已知权限边界

Telegram 服务端、聊天/频道权限和内容保护策略始终优先。应用只能读取当前 Telegram 账号与 TDLib 被允许访问的内容，不能绕过访问控制、消息转发限制、频道保护或 Telegram 的服务端风控。

如怀疑凭据或会话已经泄露，应立即在 Telegram 的活动会话中终止陌生设备、修改两步验证密码，并在 Telegram 开发者后台删除或轮换受影响的开发者应用参数。
